extends layout

block content
  .timeline
    .followings.half
      h2 Followings
      if user.Followings
        for following in user.Followings
          div= following.nick
    .followers.half
      h2 Followers
      if user.Followers
        for follower in user.Followers
          div= follower.nick